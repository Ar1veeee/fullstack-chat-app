{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "version": 2,
  "builds": [
    {
      "src": "build/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { 
      "src": "/(.*)", 
      "dest": "/build/server.js" 
    }
  ]
}